{{outlet}}

{{!--TODO:
  - replace/shift model with searchArray || model
 --}}

{{#each model as |r|}}
  {{rest-tile restaurant=r dropResto="destroyResto"}}
{{/each}}

{{rest-add model=model submitResto="saveResto"}}
